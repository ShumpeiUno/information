name: Quantum Daily Public Mirror — Retired

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Public mirror retired
        run: |
          echo "Quantum Daily generation and storage have moved to a private repository."
          echo "This public workflow no longer publishes scheduled editions."
